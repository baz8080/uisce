[project]
name = "uisce"
version = "0.1.0"
description = "Download, transform, and geocode Uisce Éireann cases"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.0.0",
    "pyproj>=3.7.2",
    "requests>=2.34.2",
]
